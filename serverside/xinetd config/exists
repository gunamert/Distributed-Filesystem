service exists
{
        port = 3007
        disable = no
        socket_type = stream
        protocol = tcp
        wait = no
        user = slave
        server = /home/slave/Desktop/py_app/exists
        log_on_failure += USERID
        log_on_success += USERID
}

