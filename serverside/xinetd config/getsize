service getsize
{
        port = 3006
        disable = no
        socket_type = stream
        protocol = tcp
        wait = no
        user = slave
        server = /home/slave/Desktop/py_app/getsize
        log_on_failure += USERID
        log_on_success += USERID
}

