service mkdir
{
        port = 3005
        disable = no
        socket_type = stream
        protocol = tcp
        wait = no
        user = slave
        server = /home/slave/Desktop/py_app/mkdir
        log_on_failure += USERID
        log_on_success += USERID
}

